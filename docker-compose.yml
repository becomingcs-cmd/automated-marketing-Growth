services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: growthos
      POSTGRES_USER: growthos
      POSTGRES_PASSWORD: growthos
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U growthos -d growthos"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - growthos_postgres:/var/lib/postgresql/data

volumes:
  growthos_postgres:

